services:

  ecole-portal:
    build:
      context: ./ecole-portal
      dockerfile: Dockerfile
    image: barssim/ecole-portal:latest
    ports:
      - "80:80"
    restart: always
    environment:
      NODE_ENV: production