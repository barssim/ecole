services:

  ecole-portal:
    build:
      context: ./ecole-portal
      dockerfile: Dockerfile
    image: barssim/ecole-portal:develop
    ports:
      - "8080:8080"
    volumes:
      - ./ecole-portal:/app
      - /app/node_modules